<?php require "vendor/autoload.php"; require_once "bootstrap/app.php"; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); $signer = \App\Models\SignerCertificate::query()->where("code", "SIGNER-001")->first(); print_r($signer->id); try { \App\Models\DigitalSignature::query()->create([ "certificate_id" => 14, "signer_certificate_id" => $signer?->id, "signed_by" => 1, "public_token" => "b", "signed_at" => now(), "signed_ip" => "1", "signed_user_agent" => "a", "is_visible" => true, "page" => 1, "pos_x" => 0, "pos_y" => 0, "width" => 200, "height" => 80 ]); echo "CREATED\n"; } catch (Exception $e) { echo $e->getMessage(); }
